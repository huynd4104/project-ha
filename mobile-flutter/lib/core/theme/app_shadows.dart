import 'package:flutter/material.dart';

class AppShadows {
  static const soft = [
    BoxShadow(color: Color(0x1F25323A), blurRadius: 18, offset: Offset(0, 8)),
  ];
  static const button = [
    BoxShadow(color: Color(0x2625323A), blurRadius: 0, offset: Offset(0, 4)),
  ];
  static const glow = [
    BoxShadow(color: Color(0x3358CC02), blurRadius: 24, offset: Offset(0, 8)),
  ];
}
