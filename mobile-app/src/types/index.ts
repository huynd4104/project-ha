export type User = { id: string; uid?: string; email: string; fullName: string; role: "PARENT" | "ADMIN"; isActive?: boolean };
export type Child = { id: string; userId?: string; name: string; age: number; gender?: string; note?: string };
export type NPC = { id: string; name: string; description: string; imageUrl: string; animationUrl?: string; defaultDialogue: string; isActive?: boolean };
export type LessonType = "MATH" | "DIALOGUE" | "FLASHCARD" | "THINKING" | "SPELLING" | "RHYME";
export type Lesson = { id: string; title: string; description: string; type: LessonType; orderIndex?: number; npcId?: string | null; npc?: NPC; progress?: any[]; isActive?: boolean };
export type Question = { id: string; lessonId?: string; questionText: string; imageUrl?: string; optionA: string; optionB: string; optionC: string; optionD: string; correctOption: string; explanation?: string; orderIndex?: number };
export type Dialogue = Question & { title: string; sceneText: string; audioUrl?: string };
export type Flashcard = { id: string; lessonId?: string; frontText: string; backText: string; imageUrl?: string; audioUrl?: string; orderIndex?: number };
export type SpellingActivity = { id: string; lessonId?: string; promptText: string; targetWord: string; letters: string[]; hint?: string; orderIndex?: number };
export type RhymeChallenge = { id: string; lessonId?: string; promptWord: string; questionText: string; optionA: string; optionB: string; optionC: string; optionD: string; correctOption: string; explanation?: string; orderIndex?: number };
